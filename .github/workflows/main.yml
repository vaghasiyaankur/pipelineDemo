name: Staging Deploy


on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: ./
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 2
        
    - name: FTP Deploy to WP Engine (Staging)
      uses: ./
      with:
        server: ftp://43.255.154.8
        username: pipeline@savanvaghasiya.info
        password: pipeline@123